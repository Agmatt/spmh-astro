---
// src/pages/dashboard/tenders/index.astro
import '../../../styles/global.css';
import TenderManager from '../../../components/TenderManager.jsx';
import TenderAuthGate from '../../../components/TenderAuthGate.tsx';

// If your other dashboard pages check auth in frontmatter (e.g. reading a
// Supabase session cookie and redirecting to /login if absent), apply the
// exact same guard here for consistency. Left out here since it depends on
// the pattern you're already using elsewhere.
---

<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Tender Manager</title>
  </head>
  <body>
    <TenderAuthGate client:load />
  </body>
</html>
